# Learn

Interactive, hands-on HighLevel workshops driven by YAML course files.

## Local development

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
npm run start
```

Course content lives in `courses/*.yaml`. Participant progress is stored locally in the browser for this version.
